# 🚀 Web Automation Platform

Многопоточная платформа для автоматизации веб-процессов с современным веб-интерфейсом управления и мониторинга.

## 📋 Описание

Профессиональная система автоматизации веб-операций, построенная на Flask и Python. Платформа предоставляет удобный веб-интерфейс для управления ресурсами, очередями задач, мониторинга выполнения и интеграции с внешними сервисами. Система поддерживает многопоточную обработку, приоритетные очереди, управление браузерными профилями и гибкую конфигурацию.

## ✨ Основные возможности

- 🔄 **Многопоточная обработка** - Параллельное выполнение задач с настраиваемым количеством потоков
- 📊 **Веб-интерфейс управления** - Интуитивная панель управления с реальным временем мониторинга
- 🎯 **Приоритетная очередь задач** - Система очередей с поддержкой приоритетов и фильтрации
- 🔐 **Управление ресурсами** - Централизованное управление аккаунтами, прокси, метками
- 📈 **Мониторинг и статистика** - Отслеживание выполнения задач и генерация отчетов
- 🔌 **Интеграции** - Поддержка внешних сервисов через REST API
- 📁 **Импорт/экспорт данных** - Загрузка данных из файлов и экспорт результатов
- ⚙️ **Гибкая конфигурация** - Настройка параметров выполнения через веб-интерфейс

## 🛠 Технологический стек

### Backend
- **Python 3.9+** - Основной язык программирования
- **Flask** - Веб-фреймворк для API и интерфейса
- **SQLite** - База данных для хранения конфигураций и данных
- **Selenium WebDriver** - Автоматизация браузера
- **asyncio** - Асинхронное программирование
- **ThreadPoolExecutor** - Многопоточная обработка

### Frontend
- **HTML5/CSS3** - Структура и стилизация
- **JavaScript** - Интерактивность и AJAX запросы
- **Font Awesome** - Иконки

### Интеграции
- **REST API** - Интеграция с внешними сервисами
- **Omnilogin API** - Управление браузерными профилями

## 🚀 Установка и запуск

### Требования

- Python 3.9 или выше
- pip (менеджер пакетов Python)
- Omnilogin (для управления браузерными профилями) - опционально

### Шаг 1: Клонирование репозитория

```bash
git clone <repository-url>
cd Software-for-rating-improvement
```

### Шаг 2: Создание виртуального окружения (рекомендуется)

```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux/macOS
python3 -m venv venv
source venv/bin/activate
```

### Шаг 3: Установка зависимостей

Установите все необходимые пакеты из файла `requirements.txt`:

```bash
pip install -r requirements.txt
```

Или установите пакеты вручную:

```bash
pip install flask requests psutil selenium
```

### Шаг 4: Настройка Omnilogin (опционально)

Если вы используете Omnilogin для управления браузерными профилями:

1. Установите и запустите Omnilogin
2. Убедитесь, что API доступен по адресу `http://localhost:35353`
3. При необходимости измените `base_url` в `src/managers/omnilogin_manager.py`

### Шаг 5: Запуск приложения

```bash
python app.py
```

Приложение будет доступно по адресу: `http://localhost:5000`

При первом запуске автоматически создастся база данных `youtube_soft.db` по схеме из `database/schema.sql`.

## 📖 Использование

### Веб-интерфейс

1. Откройте браузер и перейдите на `http://localhost:5000`
2. В разделе **"Загрузка данных"** загрузите необходимые файлы:
   - Аккаунты (формат: `email:password:recovery_email`)
   - Прокси (одна строка на прокси)
   - Видео URL (одна строка на URL)
   - Теги и названия (опционально)

3. В разделе **"Настройки"** настройте параметры выполнения:
   - Длительность просмотра
   - Максимальное количество действий на аккаунт
   - Количество потоков
   - Стратегии выбора видео
   - Дополнительные опции (лайки, подписки, поведение)

4. В разделе **"Управление"** выберите аккаунты и запустите выполнение

5. В разделе **"Очередь видео"** управляйте приоритетными задачами

### Форматы файлов

#### Аккаунты (`accounts.txt`)
```
email1@example.com:password1:recovery1@example.com
email2@example.com:password2:recovery2@example.com
```

#### Прокси (`proxies.txt`)
```
host:port:username:password
192.168.1.1:8080:user:pass
```

#### Видео URL (`urls.txt`)
```
https://www.youtube.com/watch?v=video1
https://www.youtube.com/watch?v=video2
```

## 🔌 API Endpoints

### Управление аккаунтами
- `GET /accounts` - Получить список аккаунтов
- `POST /accounts` - Загрузить аккаунты из файла
- `DELETE /accounts` - Удалить выбранные аккаунты
- `POST /accounts/<id>/tag` - Обновить метку аккаунта
- `GET /accounts/<id>/proxy` - Получить прокси аккаунта
- `POST /accounts/<id>/proxy` - Назначить прокси аккаунту

### Управление очередью
- `GET /queue` - Получить очередь задач
- `POST /queue` - Добавить задачу в очередь
- `DELETE /queue` - Удалить задачи из очереди
- `PUT /queue/<id>` - Обновить статус задачи
- `GET /queue/stats` - Получить статистику очереди

### Загрузка данных
- `POST /proxies` - Загрузить прокси
- `POST /urls` - Загрузить URL видео
- `POST /titles` - Загрузить названия видео
- `POST /tags` - Загрузить теги

### Управление выполнением
- `POST /start_script` - Запустить выполнение
- `POST /stop_script` - Остановить выполнение
- `GET /status` - Получить статус приложения
- `GET /stats` - Получить статистику выполнения

## ⚙️ Конфигурация

Конфигурация хранится в `config/config.json`. Основные параметры:

```json
{
  "watch_duration": "30",
  "max_actions_per_account": 3,
  "human_behavior": true,
  "enable_likes": true,
  "enable_subscriptions": false,
  "enable_referral": true,
  "urls_strategy": "random",
  "create_channel": false,
  "enable_title_search": false,
  "filter_strategy": "none"
}
```

## 🗄️ База данных

Система использует SQLite для хранения данных. Схема базы данных находится в `database/schema.sql`.

Основные таблицы:
- `accounts` - Аккаунты
- `proxies` - Прокси-серверы
- `video_queue` - Очередь задач
- `account_tags` - Метки аккаунтов
- `video_tags` - Теги видео
- `queue_progress` - Прогресс выполнения задач

## 🔒 Безопасность

- Храните файлы с аккаунтами и базу данных в защищенном месте
- Используйте прокси и автоматизацию в соответствии с законодательством
- Не делитесь конфиденциальными данными публично

## 🐛 Решение проблем

### Приложение не запускается
- Убедитесь, что Python 3.9+ установлен
- Проверьте, что все зависимости установлены
- Проверьте логи на наличие ошибок

### База данных не создается
- Убедитесь, что файл `database/schema.sql` существует
- Проверьте права доступа к папке проекта

### Omnilogin недоступен
- Убедитесь, что Omnilogin запущен
- Проверьте, что порт 35353 открыт
- Проверьте настройки `base_url` в конфигурации
